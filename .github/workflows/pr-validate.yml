name: PR Validate

on:
  pull_request:
    types: [edited, opened, synchronize, reopened]

permissions: {}

concurrency:
  group: ${{ github.workflow }}-edit-${{ github.event.number }}
  cancel-in-progress: true
  
jobs:
  validate:
    name: Validate PR
    permissions:
      contents: read
      pull-requests: write
    uses: bcgov/actions/.github/workflows/.pr-validate.yml@0992e41cd11c5370b77f9e6066effe63efe9f8c9 # v0.4.0
    with:
      markdown_links: |
        - [Frontend](https://${{ github.event.repository.name }}-${{ github.event.number }}-frontend.apps.silver.devops.gov.bc.ca/) available

  results:
    name: Validate Results
    needs: [validate]
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Workflow completed successfully!"
